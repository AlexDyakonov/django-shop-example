upstream shop {
    server shop:8000;
}

server {

    listen 80;

    location / {
        include proxy_params;
        proxy_pass http://mydemo-site.ru;
    }

    location /static/ {
        alias /shop/static/;
    }

    location /media/ {
        alias /shop/media/;
    }
}
